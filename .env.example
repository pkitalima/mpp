# Leave both unset to run against local IndexedDB with seeded demo data (no backend needed).
# Set both to point the same app at a Supabase project (see supabase/migrations/0001_init.sql).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
